export const formatDate = (d) => d ? new Date(d).toLocaleDateString('en-IN', { weekday: 'short', year: 'numeric', month: 'short', day: 'numeric' }) : ''; export const formatTime = (d) => d ? new Date(d).toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit', hour12: true }) : ''; export const formatPrice = (a) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(a); export const getDuration = (dep, arr) => { const diff = new Date(arr) - new Date(dep); return Math.floor(diff/3600000) + 'h ' + Math.floor((diff%3600000)/60000) + 'm'; };
